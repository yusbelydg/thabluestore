import { CartProductsFilterPipe } from './cart-products-filter.pipe';

describe('CartProductsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CartProductsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
